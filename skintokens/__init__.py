"""Pure-Python SkinTokens engine.

Wraps the vendored upstream torch core (``skintokens.vendor``) and replaces its
Blender-based glb I/O with pure-Python import/export. See ``spec/`` for the design.
"""

__version__ = "0.0.1"
